# 로컬 개발 전용 — 프로덕션 배포에는 Dockerfile 사용
FROM node:22-alpine
WORKDIR /app

RUN apk add --no-cache libc6-compat

COPY package.json package-lock.json* yarn.lock* ./
RUN npm install

ENV NODE_ENV=development
ENV PORT=8000

EXPOSE 8000

CMD ["npm", "run", "dev"]
